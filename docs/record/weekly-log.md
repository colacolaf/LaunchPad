# Weekly log

> One entry per week, no missing weeks. Sunday afternoon: write the entry + plan next week. If a week is missed, backfill honestly ("missed, backfilled") — never fake an entry.

## 2026

### Week of 2026-09-12 (Phase 1 opens)

- **Built:** the Phase 1 opening deliverable — the engine facade (`core/src/engine.rs`, ~600 lines + ~800 of tests). `Engine` owns book + ledger + a per-order `LiveOrder` map, making `place → lock → settle` atomic: place is a saga (commit → book → settle fills; book rejection releases the commit — the failure is invisible to the ledger), cancel releases the whole remaining lock dust-included, and bid moves check `free + this order's own recycled lock` before the book sees the order (asks touch no money). `EngineOutcome::KilledFok` makes a killed FOK distinguishable from a zero-fill IOC.
- **Built (2nd session, same week):** the Phase 1 scope sweep — mapped phases.md §1 ("same input → same output — there must be a test for this") against the shipped engine and closed the gaps: engine-level determinism proven by a two-run replay-digest property (sorted live-order table + all balances, folded — any divergence breaks equality); no-cross re-asserted after every engine op with a named proptest owner; and `core/tests/engine_lifecycle.rs`, a public-API integration test (full lifecycle, self-trade conservation, risk gate, market-bid reserve) replacing the hand-played-engine choreography of `order_lifecycle.rs`, which remains as the primitive-contract documentation. Also opened the Phase 1 section in docs/TODO.md (built-so-far + honest remaining list: edge-case sweep, exchange-core surface re-read, review gate, fluency gate).
- **Built (3rd session, same week):** the edge-case sweep — probes for the traps dead order ids could leave (ghost index entries, double-releasing cancels). **Finding: id uniqueness binds among LIVE orders only** — canceled, fully-filled, and killed ids leave no trace, and reuse is accepted as a fresh, independent order (venue-standard ClOrdID recycling); the probes had assumed forever-unique ids and caught the real, previously undocumented rule. Policy adopted as-is and documented on `OrderBook::place`; 7 unit tests pin all three death modes plus the no-double-release guarantee (cancel/move on dead ids error with balances untouched), and the property suite generates a `Recycle` command (place under a mostly-dead id) so the invariants hold under recycling traffic. No money hazard found: a recycled order runs the full saga with fresh locks.
- **Reviewed (4th session, same week):** the Phase 1 whole-phase review gate (code-review-and-quality, five axes over the engine facade + both sweeps + the record). Verdict: **approve**, no Critical/Required findings. Highlights: saga compensation proven by the duplicate-id path, floor-under-ceil settlement safe by subadditivity with both dusty directions regression-tested, the debit_live-before-settle ordering verified sound against the ledger's account-level settle, and all four architecture invariants now proven at engine level (determinism by replay digest, no-cross after every op, conservation, lock sufficiency) plus id-recycling traffic. Two optional findings deferred to Phase 2: engine.rs size if it keeps growing past ~1.8k lines; the replay digest could fold book depth for marginally stronger replay evidence. Verified 100/100 locally, all four gates, CI run #34877439573 green at the pushed tip.
- **Closed (5th session, same week):** **Phase 1.** The last gate fell — the user passed the fluency check unscripted (saga model + the ceil-lock/floor-settlement rule), recorded in docs/TODO.md with the exact gate items. Phase 1 marked CLOSED in docs/TODO.md with the full built-list; Phase 2 (benchmark & optimize) opened beneath it. One item folded forward deliberately: the exchange-core surface re-read becomes Phase 2's opening read — its payoff is their benchmark methodology, which is exactly what Phase 2 needs first (decision-log row). Record integrity fix in the same pass: the 2026-09-07 review row had drifted to the bottom of the decision log below newer entries; restored to chronological order per the file's own rule.
- **Built (6th session, same week — Phase 2 opens):** the Phase 2 opening read — exchange-core re-verified against primary sources (README + `IOrderBook.java` + `OrderBookNaiveImpl.java`, master, fetched 2026-09-15). Four surface findings the Aug-21 note lacked: (1) their move is *marketable* — it sweeps the opposite side at the new price, taker-style, remainder rests — while ours rejects with `WouldCross`; (2) their move gate is `price > reserveBidPrice` (reserve fixed at place), so their moves never touch balances — ours checks `free + own lock` per move, a real cost difference that will show in benchmarks; (3) a fourth operation we lack (`reduceOrder`, partial cancel → adopted for Phase 3); (4) cancel/move/reduce are uid-gated (→ adopted for Phase 4). Deliverable: the adopt/skip list in `docs/research/exchange-core.md` §re-read; decision row; methodology capture verified unchanged against source.
- **Built (7th session, same week):** the full Phase 2 setup chain — research → plan → implement → test → mistake-check. **criterion 0.8.2** adopted (`default-features = false`; rust-version 1.85→1.86), **toolchain pinned** (1.96.0) + release profile fixed (`lto = "fat"`, `codegen-units = 1`), and the **bench harness** built in `core/benches/`: a splitmix64-seeded generator reproducing the 9/3/6/82 mix exactly (mirror-verified against the engine every run), a criterion throughput target, and a harness-free latency target that times every op and proves determinism per run (two-run full-state digest equality) — plus `workload_selfcheck`, the generator's contract as a real CI-run target after discovering the `#[test]` form never executes under `harness = false`. **Baseline v1 published** (p50 291 ns / p99 709–750 / p99.9 ~1.5 µs; mixed ~3.2 M ops/s; per-op table move 265 ns → ioc 662 ns) with the methodology written BEFORE the number; the harness itself killed two dishonest numbers during bring-up (partial-fill mirror drift; a 4.7 ns empty-book cancel degenerate — refills rule adopted). Process note: crates.io index + downloads are DNS-poisoned on this network (Securly block page); worked around via cargo's git-protocol index + DoH-resolved tarball seeding — every tarball checksum-verified by cargo against the index, nothing bypassed.
- **Built (8th session, same week):** push verification + the tail widened honestly. CI green on `5e156e0` (all four jobs; the new smoke-bench job ran on a real runner for the first time: 1m18s) and delivered its first evidence — the 2k-op determinism digest `0xea9d71ec0f18e3a3` **byte-identical on the ubuntu x86_64 runner and the M1** (cross-architecture determinism), while two CI runs swung p99.9 2,845→5,010 ns, hard-proving the no-numbers-from-CI rule. Tail answer: p99.99 is now **sample-gated** (`MIN_SAMPLES_P9999 = 100_000` — ten samples above the threshold before the line prints; at v1's 50k it rested on ~5 samples, noise wearing a percentile's clothes), and **Baseline v1.1** recorded from a 1M-op run: p50 250 / p99 667 / p99.9 1,416–1,417 / **p99.99 5,459–6,583 ns** (~100 supporting samples), max ~110–136 µs (scheduler noise; percentiles, never max-only), cancel-misses 0 across 2M ops, runs 1–2 agree to the nanosecond through p99.9. v1 retained as published; v1.1 supersedes for comparisons. Decision rows: p99.99 gate + CI-proves-correctness-never-numbers. Gates after the edit: all four + `cargo bench --no-run` + selfcheck + 2k smoke re-run (smoke omits p99.99 by design — the gate works).
- **Built (9th session, same week — Phase 2 optimization #1):** profile → one hypothesis → honest verdict. Profiling methodology written BEFORE the first profile (profiles are attribution, never numbers; `sample` @1 ms over the `-g` build in a separate target dir). Two stable invocations over 20M-op runs agreed within ~0.5 pp: move_order ~29%, remove_order ~11.5%, per-op timer ~27% (measurement, excluded), hash_one ~5.2%, **best() ~4.4%** — grounded in source: `best()` was a boxed-iterator head, the Phase 0 "query path" assumption the 82%-move workload killed. H1 (de-box best() → direct BTreeMap first/last) got its prediction written before the after-run: p50 250→~235–248 ns, digest/counters/tests unmoved. **After: p50 250→250 ns, digest byte-identical, 100/100 green — H1 FAILED its prediction** and is recorded as a negative result: the share was attribution blur (sampling + LTO + allocator reuse makes a hot same-size-class box nearly free). The simplification ships with no performance claim; the written prediction is what failed, and the prediction is the record. Next target named from what survived: remove_order ~11.5% (level-structure change — scoped before any attempt).
- **Scoped (10th session, same week — optimization #2, zero code):** the remove_order target interrogated (questions/Full; user set the path, delegated the revert bar, budget 3–4 sessions). Research killed the obvious fix: at ~1.3 orders/level the queue scan is ~4 ns — a scratch micro-probe measured the real cost as **BTreeMap descents** (get_mut 11–12 ns; full move pattern 81–97 ns; deque remove ~4 ns), matching the refined profile (remove_order ~14.8% self-time, memmove 1–3%). Verdicts: B (locator/swap-remove) measured dead; C (flat array) rejected (semantics change, barred); D (ghost levels) rejected (≤ ~11 ns for a documented invariant); **A (slot arena + intrusive per-level chains) adopted** behind the unchanged public API, honest bound ~25–40 ns off p50. Revert bar written down first: keep only on **≥5% p50 at 1M ops** with digest/counters/100 tests identical; else revert-and-record. Implementation is session #3's job.
- **Built (11th session, same week — optimization #3, Option A implemented):** the slot arena + intrusive per-level chains landed fully behind the unchanged public API — outcome: the **second negative result**. The pre-compile mistake-check caught two real chain bugs in the fresh code (stale `next` links on moved orders → chain corruption; emptied-level `tail` never reset) — fixed before any run, then **100/100 tests unmodified green, digest `0xc3bea4a3…` byte-identical, counters identical, and p50 250→250 ns** (clean run: p90 334→333, p99 667→667). The pre-set ≥5% bar fired: **reverted** — book back to the Phase 0 structure, re-verified 100/100. What the three experiments compose into: isolated micro-costs (4 ns scan, 12 ns descent, alloc churn) are real but do not compose additively end-to-end — at 250 ns/op the ensemble (timer ~27% by profile, risk/lock checks, index hash, generator bookkeeping) dominates, and out-of-order execution hides tree-op latency that micro-benchmarks measure truthfully. **Plateau rule condition met (sessions #1 + #3, zero p50 movement): baseline v1.1 stands as Phase 2's honest number.** Remaining to close the phase: the v2-target confirmation row and the whole-phase review.
- **Confirmed & reviewed (12th session, same week — Phase 2 closes its mandate):** the v2 target confirmed against the measured baseline (500k ops/sec target vs ~3.2 M measured — 6.4× over; p99 667 ns vs the <100 µs bar; determinism proven cross-architecture; no new stretch targets invented — target-shopping barred) and the whole-phase review gate passed (five axes over the full Phase 2 diff: **approve, no Critical/Required**; two optional findings deferred — decompose `moves_rejected` causes, lift bench constants at a third consumer; Phase 1's digest-depth carry-forward stays open for Phase 3; the reverted arena left zero debt). Phase 2's Done-when triple is met: harness runs reproducibly, methodology published, honest number recorded (v1.1).
- **Closed (13th session, same week):** **Phase 2.** The fluency gate passed (user-declared, self-administered — all three items: the disclosed per-op scope + the comparability disclosure; the two negative results and the three-experiment conclusion; digest-proven determinism + the CI-asserts-correctness-never-numbers rule; exact phrasing appendable on request). Phase 2 CLOSED in docs/TODO.md with the gates list and v1.1 as the honest number; **Phase 3 (risk + event sourcing) opened** — fees and position limits on the existing integer ledger, `reduceOrder` (the adopt-deferred item), disk journal + snapshots + replay, Done-when: the replay-produces-identical-state test. Carry-forwards landed in the Phase 3 section; the bench digest machinery is the replay-test precedent. README status → Phase 3.
- **Built (14th session, same week — Phase 3 opens):** the ledger audit + the fees plan. Audit verdict: the phases.md Phase 3 list is already half-built — balances + the over-commitment gate ARE the Phase 0/1 ledger (reserve-at-place, ceil-lock/floor-settle with the subadditivity proof, conservation property); missing are position limits, fees, journal/snapshots/replay, `reduceOrder`. The audit caught one scope drift: book.rs's header claimed self-match prevention as Phase 3 work the phases.md never asked for — ruled out of Phase 3 (→ Phase 4, with uid gating), while self-trade FEE treatment (fees on both receipts) was defined now. **Fee model adopted** (decision rows): fees in the RECEIVED asset (no extra lock headroom needed — the alternative forces role-dependent headroom into place-time locks for a bid that can fill partly taker/partly maker), integer bps ≤ 10_000, per-currency fee sink, conservation extended to Σ(free+locked)+Σ(fees)=deposits, floor rounding (never over-charge), self-trades pay, benches stay 0-fee (v1.1 comparability), schedule journaled for replay. Full test plan in tasks/todo.md — the fees slice is the next session's contract.
- **Built (15th session, same week — the fees slice):** decision row 52 implemented end-to-end — `FeeSchedule` in risk.rs (integer bps validated ≤ 10_000 at construction, split-multiply overflow-proof fee math, floor rounding), the per-currency fee sink (`collect_fee` / `fees_collected`; conservation → Σ(free+locked)+Σ(fees)=deposits), and engine fee legs after both settle legs **dispatched on `taker_side`** (self-trades pay fees on both receipts; dispatch on the side, not user equality — the equality form misfires when both sides are one user). `Engine::new` stays zero-fee; `Engine::with_fees` opts in; fees never touch locks by construction (the lock covers the paid asset, the fee comes from the received asset). Verified: **106 lib tests** (6 new fee tests) + fee'd property twins green; **0-fee byte-identity proven** — canonical 1M-op digest `0xc3bea4a3b66dd253` and every realized counter identical to v1.1. The post-edit gates caught **3 bugs in the fresh code before commit** (decision row 53): a botched edit had clobbered `settle`'s payee-credit branch (invisible to the new fee tests, flagged instantly by conservation in pre-existing tests; repaired byte-identical from HEAD), the overflow test's expected constant was 100× off, and the first fee-leg draft charged both fees on base lots instead of keying the seller's fee on `quote_amount` ticks.
- **Built (16th session, same week — three slices: limits, reduce, fee'd bench):** the remaining Phase 3 pre-journal scope in one pass, each decision written before its code. **Position limits** (row 54): per-(user, currency) caps on LOCKED funds — the capped risk is open-order exposure, not wealth — gated inside `Ledger::commit` before any mutation (the only lock-growth choke point, so uncapped paths cannot bypass it), default uncapped, `cap=0` a legal freeze, and a move-top-up pre-check guaranteeing the release-then-commit path can never strand an order lockless. **`reduceOrder`** (row 55): the Phase 2 adopt-deferred op — clamp-to-remaining, full-reduce = exact cancel incl. dust, partial keeps queue position (proven through fill order, the book's only public priority surface); money rule mirrors a fill of the same size (bid floor cost / ask exact lots; the dust convention deliberately the SAME as settlement — one rounding story). **Fee'd bench mode** (row 56): `LAUNCHPAD_BENCH_FEES="m,t"` at engine construction, default byte-identical (1M digest re-proven `0xc3bea4a3b66dd253`); same-session 1M comparison 0-fee vs 100% fees: p50 250 → 250 ns, the written prediction confirmed; and the honest find that at 10/25 bps the fee'd digest EQUALS 0-fee — bench fills floor realistic fees to zero (the floor rule working by design). 123 lib + 4 + 3 integration green; catches this session: a literal `\n` typo inside book.rs (read-before-compile caught it), mis-scaled first-draft limit tests (rewritten on the suite's house scales), one test-side arithmetic slip (the engine was right).
- **Reviewed (17th session, 2026-09-17 — Phase 3 mid-phase review gate):** code-review-and-quality, five axes over the full Phase 3 diff `1e2bcb2..9514560` (fees + limits + reduce + fee'd bench + records), tests read first. Verdict: **approve, no Critical/Required.** Correctness clean (money mutations pre-checked or expect-guarded with the invariant named at the scene; fee legs dispatched on `taker_side` — self-trades correct by construction; reduce clamp at the book's invariant site; the full edge matrix in tests: cap=0 freeze, cap-below-lock, at-cap pass, dusty-lock splits, `u64::MAX` fee, 10_000 bps boundary); architecture clean (risk rules in the ledger, orchestration in the engine, the book money-blind; bench fee config one wiring point, fail-fast); security clean (no new deps, env config validated + loud panic); performance clean (gate + fee legs inside the measured canonical run — digest `0xc3bea4a3b66dd253`, p50 250 ns unchanged). Two optional findings deferred with standing instructions: **decompose engine.rs (2,216 lines) / risk.rs (1,255) at the journal slice's seam before piling on**, and a third money-in-motion path must extract the shared would-lock rule rather than copy commit's pre-check again. Digest-depth carry-forward now load-bearing for the journal slice.
- **Built (18th session, 2026-09-17 — review findings executed):** the mid-phase review's one actionable finding, done the same day. Triage: finding 1 (size) had a standing instruction — decompose at the journal slice's seam *before* piling on — so the test modules moved out verbatim (`engine/tests.rs` 1,497 lines, `risk/tests.rs` 643; engine.rs 2,216→719, risk.rs 1,255→612, all four under the size signal; one dedent, no content edits, same `mod tests` path). Behavior-invisibility proven: fmt/clippy clean, 123 + 4 + 3 tests identical, and the 2k-op digest byte-identical pre- vs post-split (`0xd736d89a4102f8b4`, measured against same-session HEAD via stash, not a stale number). Finding 2 consciously not executed — its trigger is a third money-in-motion path; only two exist, and extracting now would break the third-use rule — recorded as the standing trigger in decision row 58.
- **Built (19th session, 2026-09-17 — the journal slice, the Done-when):** the command journal landed — `core/src/journal.rs` (455 lines) + `journal/tests.rs` (392, split convention from day one). Commands-not-events per the pre-written plan (decision rows 59–60): the journal records the engine's full input surface + a header (pair + fee schedule) + **every outcome, acceptance and rejection alike** — `record` is infallible by protocol after the scripted rejection test exposed the first draft's incoherent contract (trading rejections recorded, money rejections escaping as `Err` = unrecorded state change). Replay runs commands through the ONE engine; `JournalError::Divergence` fires loudly on any acceptance disagreement. **The Done-when is proven:** `replay_of_random_commands_produces_identical_state` — up to 120 biased random commands record → replay → deep-digest byte equality, where the deep digest folds config + all accounts + fee sink + caps + live orders + **book depth with per-level queue order** (the Phase 1 digest-depth carry-forward, closed via the new `OrderBook::depth`/`Depth` public surface). Engine untouched: 2k digest `0xea9d71ec0f18e3a3` byte-identical, 131 + 4 + 3 tests green. The gates caught the first draft honestly (4/8 failing): scenarios never funded BTC (asks lock base) and one sanity assert was wrong. Remaining for Phase 3, explicit: disk byte format + snapshots (row 59's recorded deferral — the phase does not close until disk lands).
- **Built (20th session, 2026-09-17 — disk journal + snapshots, the deferral paid):** the framed `LPDJ0001` disk format landed (magic + header riding in the file + length-prefixed entries, all-u64 LE, zero deps) with a **total** decoder — corruption of any kind surfaces as `JournalError::Corrupt`, never a panic; snapshots landed as `Engine::capture_snapshot`/`restore_snapshot` with **validate-all-then-mutate** all-or-nothing and **exact** state-setting (the dust-bearing bid lock cannot be reproduced by re-placement — the mistake-check killed the first `relock` draft as conservation-breaking), the captured lock column re-derived from per-order rows rather than trusted, and `Journal::replay_from` composing recovery (snapshot + trailing commands) — **proven equal to full replay by deep digest on randomized streams**. `book.rs` also decomposed (1,276 → 814 + sibling tests). 16 journal tests total, 139 + 4 + 3 green, 2k digest `0xea9d71ec0f18e3a3` byte-identical. Two honest corrections during implementation: freeze-caps accepted at restore (row 54's documented semantics) and no locked≤free check (a false invariant — locked>free is legitimate post-settlement state).
- **Reviewed (21st session, 2026-09-17 — Phase 3 whole-phase review gate):** code-review-and-quality, five axes over the full journal work `1fefdc5..fc880a6`, tests read first. Verdict: **approve, no Critical/Required.** Restore validation re-derives every invariant (lock-sum, order-independent no-cross, duplicates, symbol, resting shape); mutation infallible by construction; zero new deps; trading paths digest-identical across all three journal commits. Two findings fixed in-review: `restore_depth`'s no-cross check hardened from first-row-only to order-independent max/min (public method must stand alone), and book.rs's header no longer claims balance checks as a gap (shipped in Phase 3; self-match prevention remains the sole gap → Phase 4). **Remaining to close Phase 3: the fluency gate.**
- **Tests (through the bench work):** 100/100 green (93 lib + 7 integration) — bench targets are not tests, so the generator's contract runs as the `workload_selfcheck` CI target instead. All four gates (fmt, clippy `-D warnings`, test, release smoke) plus `cargo bench --no-run` and the 2k-op latency smoke. The three Phase 0 review flags are each closed by code + test: (a) bid-move lock top-up, (b) `KilledFok`, (c) the widened property strategy.
- **Decided (see decision-log rows 2026-09-14..15):** floor-per-fill settlement with ceil locks (per-fill ceil could settle MORE than the lock — the dusty `3 lots @ 3 ticks` case would panic `Ledger::settle`; floor is subadditive-safe and the dust returns to free at order death); market bids require a reserve and are rewritten to Limit-IOC at it (exchange-core's `reservePrice` semantics); a reserve anywhere else is rejected; exchange-core re-read adopt/skip verdicts (2026-09-15 — methodology + move-heavy workload adopted, `reduceOrder` + uid gating adopted-deferred, marketable moves + FOK-B skipped).
- **Caught:** the property suite's cancel/move arms were silent no-ops — `live_ids` was declared, indexed, and filtered but never pushed to, so `get(...)` on an always-empty list selected nothing and only places ever ran. Fixed by tracking rested ids at place; the randomized suite now genuinely exercises the cancel and move sagas (including lock top-ups and dust releases) after every operation asserts conservation + per-order lock sufficiency.
- **Missed / plateaus:** none. Quiet stretch 09-08..09-13 (five days, no project activity after the 09-07 plan commit) — not a skipped week, so no backfill entry; noted here for honesty.
- **Next week:** Phase 3 opens — audit the ledger vs the phases.md scope (balances exist; position limits + fees are new), then fees (settle-path fee leg + extended conservation) and `reduceOrder` (proportional lock release + reduce event). Journal/snapshot/replay follow; the replay-determinism test is the phase's Done-when.

### Week of 2026-08-28 (missed, backfilled 2026-09-07)

- **Built:** nothing — no project activity between the 2026-08-21 setup week and the 2026-09-05 Phase 0 kickoff (the decision log shows zero entries in this window).
- **Missed / plateaus:** no entry was written that week. Backfilled per this file's own rule ("if a week is missed, backfill honestly — never fake an entry"); the gap was inactivity, not unrecorded work.

### Week of 2026-09-05 (Phase 0 kickoff)

- **Built:** Rust workspace scaffold — virtual cargo workspace (resolver 3), `launchpad-core` crate (edition 2024, zero deps, smoke test), shared `[workspace.lints]` (unsafe forbid, missing_docs warn), rustfmt style_edition 2024, placeholder `bench/`/`sim/`/`venue/`, 4-job GitHub Actions CI (fmt / clippy `-D warnings` / test / release smoke), README getting-started + live CI badge. All four gates verified locally before commit.
- **Built (3rd session, same week):** the Phase 0 order book (`core/src/book.rs`) — `OrderBook` with place/cancel/move_order, best-bid/best-ask accessors, limit/IOC/FOK/market semantics, `Fill` events at maker prices; 28 unit tests + the four property invariants (price-time, no-cross, per-side conservation, determinism) via `proptest`. **The property suite earned its keep immediately:** it caught a real index-leak bug (fully-filled makers left in the id→locator index) plus a conservation-accounting subtlety (fills consume from both sides). First real CI run #33991924373 all green — badge proven.
- **Built (4th session, same week):** the TODO §2/§3 learning curriculum in `docs/learning/` — a Socratic walkthrough of the real code (8 lessons: split borrows, Copy semantics, the strict parser, enum modeling, the failure ladder, visibility, traits incl. `Box<dyn Iterator>`, and the full microstructure story), a 28-question closed-book self-test with the §9 exit-gate exercise and rubric, and a cited answer key. Every claim empirically verified by temporary audit tests — which caught **two errors in the tutor's own worked examples** (a fill-arithmetic slip and a wrong "remainder rests" claim; the corrected extension question — *why place can never rest into a lock* — is now stronger than the original). CI re-proven on the order-book push (run #34047751858, all four jobs green).
- **Built (5th session, same week):** the §5 risk/accounting slice — `core/src/risk.rs` integer ledger with **reserve-at-place** semantics (the design fork settled by primary-source research: exchange-core's place carries `reservePrice`; check-only lets two GTC bids over-commit one balance). `Ledger` (deposit/withdraw/commit/release/settle, all checked, overdraft unrepresentable), `CurrencyId` domain newtype, ceil-rounded `quote_cost_ticks` with the units derivation documented. 18 unit tests + a randomized ledger-conservation property (model-vs-ledger agreement after every op) + 3 integration tests in `core/tests/` playing the engine: full lifecycle (deposit → commit → fill settles both currencies → cancel releases), risk gate rejects before the book, ceil dust returned. 64/64 green, all gates. Phase 0 code deliverables now complete; remaining: §2/§3 fluency gates, §4 reading, final review, exit gate.
- **Built (2nd session, same week):** §5 domain model in `core/src/domain.rs` — `Side`, `OrderType`×`TimeInForce` (validity enforced at construction), `OrderAction`, scaled-integer `Price`/`Qty` with strict decimal parsing + exact `Display` round-trip, id newtypes, engine-timestamped `Order`. 18 unit tests covering every rejection path. All gates green (build/fmt/clippy `-D warnings`/18 tests).
- **Built (6th session, same week):** the whole-phase review (TODO §7 gate) — five axes over all Phase 0 code + docs. Verdict: **approve**, no Critical/Required code findings. Fixed: stale README status, one dead test artifact (tautological `OrderAction` assert in `book.rs` tests), `Account` doc invariant. Record integrity: 12 decision-log rows consolidated into the main chronological table; week of 2026-08-28 backfilled. Three findings flagged for the Phase 1 engine facade (upward bid moves need a lock top-up; FOK-kill needs a distinguishable outcome; property strategy should widen beyond GTC limits). All four gates re-run green (64/64).
- **Exit gate (7th session, same week):** §9's last box — the user explained price-time priority unscripted: price half first ("best available price ... highest offer from the buyer and the lowest from the seller, are first"), time half nailed on re-ask ("same price → earliest arrival executes first"). Graded honestly: both halves correct, own words. **Phase 0 closed; Phase 1 (matching engine v1) opened.**
- **Researched:** Cargo Book workspaces + `[workspace.lints]` (primary); `actions-rust-lang/setup-rust-toolchain@v1` + Swatinem/rust-cache (primary docs); corrode.dev CI tips (secondary). Two disagreements resolved and logged: commit `Cargo.lock` (yes, for reproducibility); `rust-toolchain.toml` pin (no for now — local cargo is Homebrew, revisit Phase 2).
- **Benchmarks:** none (Phase 0 excludes them; CI runs a release-profile smoke job only).
- **Decided:** see `decision-log.md` — workspace scaffold row (2026-09-05).
- **Missed / plateaus:** none. Note: first real CI run still unproven until the next `git push` — badge live but not yet earned.
- **Next week:** TODO §5 domain model — `Side`/`OrderType`/`OrderAction` enums, integer price/qty representation, newtypes, `Order` struct; every item with a test.

### Week of 2026-08-21 (setup week)

- **Built:** Repo scaffolded — AGENTS.md, docs/ (plan, architecture, stack, benchmarks, phases, guardrails, research notes on exchange-core/ABIDES/LEAN/IMC, record templates, paper/venue/internships). Skills installed in-repo.
- **Researched:** Verified the four reference projects (exchange-core 2,602★, ABIDES 560★, LEAN 21,287★, IMC Prosperity 18,803 teams). exchange-core benchmark methodology captured.
- **Decided:** see `decision-log.md`.
- **Next week:** Phase 0 prep — Rust crash course, order-book mechanics, minimal order book with unit tests.

---

## Template

### Week of YYYY-MM-DD

- **Built:** [what was built / changed this week]
- **Researched:** [what was studied]
- **Benchmarks:** [numbers + methodology pointer, if any]
- **Decided:** [link to decision-log rows]
- **Missed / plateaus:** [honest note if anything slipped]
- **Next week:** [one-line goal from the phase table]
