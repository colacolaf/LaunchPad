# Decision log

> Append-only. One line per decision/session, ~2 min. Every design choice, every paper trade, every miss. The record starts today.

| Date | Decision | Rationale | Status |
|---|---|---|---|
| 2026-08-21 | Project: Launchpad — matching engine + simulator + hosted competition | Most AI-proof, interview-proof, dual-purpose (portfolio + internships) | Adopted |
| 2026-08-21 | Stack: Rust core + Python sim — committed (write-both-then-pick rule dropped) | Learning-depth is the #1 axis; building from scratch in Rust (no mature Rust reference to copy) deepens learning; compile-time data-race safety + no-GC hot path; strongest interview story. Deep-research pass confirmed the recommendation; the original Phase 0 write-a-minimal-order-book-in-both-Rust-and-Java comparison was judged unnecessary and dropped. Java deferred — only revisit if side-by-side benchmark parity with exchange-core becomes the top priority. | Adopted |
| 2026-08-21 | Public repo from day 1; decision log weekly; quarterly write-ups | The 12-month record (C) is a core deliverable, not a nice-to-have | Adopted |
| 2026-08-21 | Repo scaffolded: AGENTS.md, docs/ (plan, architecture, stack, benchmarks, phases, guardrails, research, record, paper, venue, internships) | Self-contained context for future coding agents; public-safe (no personal data) | Adopted |
| 2026-08-21 | Skills installed in-repo: deep-research, college, questions (personal) + code-review-and-quality, rust-best-practices, rust-testing (ecosystem) | The working skill set for research, planning, and code quality | Adopted |
| 2026-08-21 | Project name: "Launchpad" (lowercase p) | Working title confirmed; docs standardized to "Launchpad". Filesystem dir + GitHub remote are camelCase "LaunchPad" — rename remote repo separately if desired. | Adopted |
| 2026-08-21 | Competition timing | Deferred to Phase 4/5 planning; revisit once the venue exists. Options: Apr–May 2027 (align w/ IMC Prosperity) vs March standalone. | Deferred |
| 2026-08-21 | Paper venue (B): SSRN-style preprint as primary | Preprint fits the anti-vibe-coding, reproducible-first posture; no acceptance gate. 2-3 additional target journals/competitions researched at Phase 7. | Adopted |
| 2026-08-21 | License: MIT | Not commercializing (no path to sell), so no reason to forbid commercial use — permissive license, no friction. MIT over Apache-2.0 for simplicity + consistency with bundled skills; patent clause (Apache) irrelevant for a learning artifact. Matches reference repos being permissive (Apache-2.0 / BSD-3). | Adopted |
| 2026-09-05 | Domain model: `OrderType {Limit, Market}` × `TimeInForce {Gtc, Ioc, Fok}` instead of TODO §5's five flat variants | Limit and GTC were redundant as variants, and Market+GTC is a contradiction; the orthogonal pairing lets validity be enforced at construction (`OrderType::for_tif`). Market+FOK rejected for Phase 0 (exotic; revisit Phase 1 if the simulator needs it). TODO wording deviation flagged. | Adopted |
| 2026-09-05 | Scaled-integer constants: `PRICE_SCALE = 10_000` (4dp), `QTY_SCALE = 100_000_000` (8dp, satoshi-style), global for Phase 0 | 8dp matches the finest common subdivision (BTC) so quantity strings rarely need rejecting; per-symbol scales deferred to Phase 3. Strict decimal-string parsing, no `f64` in the crate (exchange-core rule). | Adopted |
| 2026-09-05 | Hand-rolled `DomainError` enum; `thiserror` deferred | Four variants are eyeball-auditable; the dependency lands when the error surface grows — dependency decisions logged one at a time. | Adopted |
| 2026-09-05 | Workspace scaffold: virtual cargo workspace (resolver 3), `core` crate, shared `[workspace.lints]` (unsafe forbid, missing_docs warn), rustfmt style_edition 2024, 4-job CI (fmt/clippy/test/release smoke), `Cargo.lock` committed | One source of truth for metadata+lints; compile-time enforcement of the Phase 0 no-unsafe rule; the four CI jobs mirror the four local gates in TODO §6. Cargo.lock committed for reproducibility (current Cargo guidance overrides the old "ignore for libs" rule). No rust-toolchain.toml pin yet — local cargo is Homebrew, pin would bind CI only; revisit at Phase 2 with benchmark methodology. Empty `.cargo/config.toml` skipped (dead weight until a flag exists). | Adopted |
| 2026-09-06 | Order book structure: `BTreeMap<Price, PriceLevel>` per side, FIFO `VecDeque` per level, `HashMap` id→locator index | Correctness-first Phase 0 choice: O(log P) best-price, queue position IS time priority, no unsafe/dependencies. Arenas/lock-free are Phase 2 per TODO §10. | Adopted |
| 2026-09-06 | `move_order` rejects repricing into the opposite side (`WouldCross`); same-price moves re-queue at tail (priority reset) | A move is the one op that could create a locked/crossed book silently — rejected Binance-amend-style instead. The no-cross invariant is non-negotiable. Method named `move_order` (`move` is a Rust keyword). | Adopted |
| 2026-09-06 | FOK is quantity-based (all-or-nothing on lots), not exchange-core's FOK-B budget variant | TODO §6 wording says "fill entirely or not at all"; budget-FOK deferred. Deviation from the reference flagged. | Adopted |
| 2026-09-06 | `proptest` added as the crate's first dev-dependency | The four §6 invariants (price-time, no-cross, conservation, determinism) demand randomized sequences; unit tests alone never cancelled a filled id — the property suite caught a real index-leak bug (fully-filled makers left in the id index; regression test added). | Adopted |
| 2026-09-06 | Learning curriculum shipped as docs/learning/ (walkthrough + closed-book self-test + separate answer key) for TODO §2/§3 | Evidence-based method: worked examples first (novice stage), then retrieval practice (Roediger & Karpicke 2006: 61% vs 40% retention at 1 week), generation effect (out-loud answers), spacing (day-2/day-7 re-quiz). §2/§3 boxes tick only after the day-7 pass. The §2/§3 checklists measure *the user's* fluency — they are not tickable by shipping AI-built code. | Adopted |
| 2026-09-06 | Risk ledger uses reserve-at-place (commit/release/settle), not check-at-place | Check-only lets two GTC bids each pass against one free balance — both can fill, user owes the exchange. Reserve makes over-commitment structurally impossible (u64 free/locked, checked math). exchange-core's place command carries `reservePrice` precisely so move needs no risk re-check — the reference architecture confirmed by primary source. | Adopted |
| 2026-09-06 | Quote-cost rounding: ceil on locks (dust released on cancel/fill-completion); `quote_cost_ticks` is the module's public pricing primitive | Under-charging a lock would let a fill cost more than was reserved; ceil over-charges by <1 tick and returns the dust, so no user loses value. Per-symbol scales (which make costs exact) stay Phase 3. | Adopted |
| 2026-09-06 | No engine facade in Phase 0: book + ledger are separate components; `core/tests/order_lifecycle.rs` scripts both and plays the engine | Risk is its own component per architecture.md; wiring place→lock→settle atomically is the Phase 1 opening task. Integration tests document the exact engine contract (commit before place, settle from locks, release on cancel). | Adopted |
| — | Benchmark targets final (v2 = 500k ops/sec) | Calibrated to exchange-core reference | Confirm at Phase 2 |
| 2026-09-07 | Whole-phase review passed (TODO §7/§9 gate); record consolidated — 12 decision rows that had been appended after the "How to add an entry" section moved into this main chronological table; week of 2026-08-28 backfilled in the weekly log per its own rule | Five-axis review over all Phase 0 code + docs (code-review-and-quality skill): **approve**, no Critical/Required code findings — invariants are structural, the property suite has caught two real bugs, integration tests document the Phase 1 engine contract. Fixed in the same commit: stale README status, one dead test artifact (tautological `OrderAction` assert), `Account` doc invariant rewritten. **Flagged for the Phase 1 engine facade:** (a) an *upward bid move* increases the quote commitment — move needs a lock top-up (exchange-core's `reservePrice` is a worst-case reserve, which is why their move skips the risk check); (b) a killed FOK is indistinguishable from a zero-fill IOC in `PlaceOutcome` — the engine will want a reject signal; (c) the property strategy covers only GTC limit orders — widen it when the engine lands. | Adopted |
| 2026-09-14 | Phase 1 engine facade implemented: `Engine` owns book + ledger + live-order map; place/move/cancel are sagas with guaranteed-succeeding compensations | Makes `place → lock → settle` atomic (the Phase 1 opening task): a book rejection releases exactly what was committed, a killed FOK reports `KilledFok` (review flag (b) closed), bid moves check `free + own recycled lock` before any money moves (flag (a) closed), and the property strategy now covers GTC/IOC/FOK/market-with-reserve places + cancels + moves (flag (c) closed). Caught in this session: the suite's cancel/move arms were silent no-ops (`live_ids` was never populated) — fixed, so the randomized paths now genuinely exercise the cancel and move sagas. 86 tests, all four gates green. | Adopted |
| 2026-09-14 | Settlement rounding: settle per fill with FLOOR (`quote_cost_floor_ticks`); locks keep their ceil | Ceil is subadditive (`Σ ceil(xᵢ) ≥ ceil(Σ xᵢ)`), so per-fill ceil can settle MORE than the single-ceil lock — 3 lots @ 3 ticks locks 1 tick, three 1-lot fills would "owe" 3 and panic `Ledger::settle`. Floor is safe in the other direction (`Σ floor ≤ ceil(Σ) = lock`); un-settled dust stays in the lock and returns to free when the order dies; sellers take a sub-tick haircut until per-symbol scales (Phase 3) make settlement exact. | Adopted |
| 2026-09-14 | Market bids require a reserve price: `Engine::place` rewrites Market+Bid → Limit-IOC at the reserve | exchange-core's `reservePrice` is the reference answer, and a market bid with reserve R is *semantically* a Limit-IOC at R — it funds the lock and bounds every fill's settlement (maker prices ≤ R). Market asks need none: their lock is base lots and quote received is never owed. A reserve anywhere else is `UnexpectedReserve`. | Adopted |
| 2026-09-14 | Phase 1 scope sweep: the engine-level test suite now owns all four architecture invariants — determinism added as a two-run replay-digest property, no-cross re-asserted after every engine op with a named proptest, plus a public-API integration test (`core/tests/engine_lifecycle.rs`) | phases.md §1 says "same input → same output — there must be a test for this"; until now the engine inherited determinism only structurally (u64 seq, no clocks, BTreeMap/VecDeque) without proving it at its own level. Digest = sorted live-order table + all balances, folded; any divergence in any field breaks equality. Price-time priority stays proven at the book (where it lives) and is composed unchanged by the engine. 92 tests, all four gates green. | Adopted |
| 2026-09-14 | Edge-case sweep finding ADOPTED: order ids are unique among LIVE orders only — a canceled, fully-filled, or killed id leaves no trace and may be recycled as a fresh, independent order (venue-standard ClOrdID recycling); duplicates of live ids are rejected with full saga compensation; cancel/move on a dead id always errors and never double-releases | The sweep's probes expected forever-unique ids and caught the real rule instead: the book's duplicate gate is `is this id live now`, not `was it ever seen` — and the filled-maker index removal from Phase 0 is exactly what makes recycling safe. Policy adopted as-is and documented on `OrderBook::place`; 7 new unit tests pin all three death modes (recycle accepted, live-duplicate rejected, dead-id cancel/move erroring with balances untouched), and the engine property suite now generates a `Recycle` command (place under a mostly-dead id) so conservation, lock sufficiency, and engine/book agreement hold under recycling traffic. No money hazard found: a recycled order runs the full saga with fresh locks. 100 tests, all four gates green. | Adopted |
| 2026-09-14 | Phase 1 whole-phase review passed (docs/TODO.md Phase 1 gate) — **approve**, no Critical/Required findings; the phase's remaining gate is the user's fluency pass | Five-axis review over the engine facade + both test sweeps + the risk/book additions + the record. Correctness: every saga path compensated (duplicate-id proof), floor-under-ceil settlement is subadditivity-safe with both dusty directions regression-tested, the debit_live-before-settle ordering is sound against the ledger's account-level settle (dust + fill exactly consume the lock, never negative), zero-size orders unrepresentable (domain rejects zero), zero-fill IOC releases through the normal remainder path, symbol mismatch gated before the saga. All four architecture invariants proven at engine level (determinism by replay digest, no-cross after every op, conservation, per-order lock sufficiency) plus id-recycling traffic in the property suite. Security: overflow → reject, never wrap; panics only on internal invariants, never user input; u64 locks make overdraft unrepresentable. Performance: out of scope by mandate (Phase 2). **Optional findings deferred:** (i) `engine.rs` at 1,796 lines (≈800 impl / ≈1,000 tests) — in-module tests are the crate's stated convention; revisit only if Phase 2 bench work grows it further; (ii) the replay digest could also fold book depth/best prices for marginally stronger replay evidence (engine/book count-agreement already asserted; the book's own determinism property composes). Verified: 100/100 local, four gates, CI run #34877439573 green at the pushed tip. | Adopted |
| 2026-09-14 | **Phase 1 closed; Phase 2 (benchmark & optimize) opened.** Closing gates all passed: the whole-phase review (approve) and the fluency gate — the user explained, unscripted, the saga model (each step's inverse is guaranteed to succeed) and the settlement-rounding rule (`Σ floor ≤ ceil(Σ) = lock`). One Phase 1 item deliberately folded forward: the exchange-core surface re-read becomes Phase 2's opening read | The re-read's payoff is exchange-core's *benchmark methodology* — exactly what Phase 2 opens with ("copy verbatim in spirit"), so it lands where it is used. Phase 2 rule zero stands (docs/benchmarks.md): no number published without methodology. | Adopted |
| 2026-09-15 | Exchange-core re-read against the finished engine surface (Phase 2 opening read) — **ADOPT:** the benchmark methodology (mix, no-burst, no-coordinated-omission, HDR percentiles, hardware disclosure — capture verified unchanged against source) and the move-heavy 82% workload weighting; **ADOPT-DEFERRED:** `reduceOrder` (partial cancel → Phase 3, it is money-relevant) and uid ownership checks on cancel/move (→ Phase 4, load-bearing only with untrusted input); **SKIP:** marketable moves (core-semantics change = feature, and Phase 2 is performance-only; composable as cancel+place), `reservePrice` as a replacement for our move-time check (ours is more flexible; revisit only if profiling points there), FOK-B (quantity-based FOK matches the mandate), the duplicate-id partial-match nuance (ours is stricter, with guaranteed compensation) | Primary-source re-read (README + `IOrderBook.java` + `OrderBookNaiveImpl.java`) found four surface facts the Aug-21 note lacked: their move is *marketable* (taker sweep at the new price, remainder rests) vs our `WouldCross` rejection; their move gate is `price > reserveBidPrice` so moves never touch balances — ours checks `free + own lock`, a real per-op cost difference that **will** show in benchmarks; they have a fourth operation we lack; cancel/move are uid-gated. New standing rule for published numbers: a comparability disclosure (our per-op cost includes risk handling theirs defers to a place-time reserve) sits next to the reference table. | Adopted |

| 2026-09-15 | **criterion adopted** as core's second dev-dependency (after `proptest`) — 0.8.2, `default-features = false` (drops plotters + rayon; keeps numbers/CSV via `cargo_bench_support`) | Lean build, numbers are what we publish not plots; requires rust ≥1.86 → workspace `rust-version` bumped 1.85→1.86, one decision for one reason | Adopted |
| 2026-09-15 | **Toolchain pinned** (`rust-toolchain.toml` → 1.96.0) and **release profile fixed** (`lto = "fat"`, `codegen-units = 1`) — the two reproducibility decisions deferred from Phase 0 | Benchmarks are comparable only under a fixed compiler + fixed codegen; honest caveat: local Homebrew rust ignores the pin (no rustup), so the environment fingerprint discloses the compiler actually used per number | Adopted |
| 2026-09-15 | **Baseline v1 published** (first number of the project) — p50 291 ns / p90 375 / p99 709–750 / p99.9 ~1.5 µs for the 9/3/6/82 mix at ~1,000 live orders, M1 laptop, full client-to-ack scope; methodology written into docs/benchmarks.md BEFORE the run; every run proves determinism (two-run digest equality) + conservation + mirror/engine agreement, and aborts rather than publishes on violation | Rule zero made real: no number without methodology; the harness itself found and killed two dishonest numbers during bring-up (partial-fill mirror drift, 4.7 ns empty-book cancel degenerate) — both fixed by rule, not tuning | Adopted |
| 2026-09-16 | **p99.99 is sample-gated, never aspirational** — the latency harness prints it only at N ≥ 100,000 (`MIN_SAMPLES_P9999` in `core/benches/latency.rs`: ten samples must sit above the threshold before the number means anything); **Baseline v1.1 published** with it (1M ops: p99.99 5,459/6,583 ns on ~100 supporting samples), v1 retained as published | At 50k ops p99.99 rested on ~5 samples — noise wearing a percentile's clothes; omitting the line beats printing noise, so the gate is code, not policy | Adopted |
| 2026-09-16 | **CI runners prove correctness, never numbers** — the smoke bench asserts (self-check, determinism digest, conservation) and records no latency; its evidence value demonstrated day one: the 2k-op digest `0xea9d71ec0f18e3a3` matched M1 and the ubuntu x86_64 runner byte-for-byte (cross-architecture determinism), while two CI runs on the same shared VM swung p99.9 2,845→5,010 ns | Shared-runner VMs have uncontrolled noise a laptop doesn't; publishing CI numbers would cherry-pick environment, so the methodology's numbers-from-one-disclosed-machine rule is enforced by what CI is allowed to print | Adopted |
| 2026-09-16 | **Profiling methodology adopted** (docs/benchmarks.md): profiles are attribution, never numbers — `sample` @1 ms over the `-g` instrumented release build in a separate target dir; one hypothesis per session with the prediction **written before the after-run**; failed predictions are recorded as results, never retried until they flatter | Attribution before optimization; a before/after claim without a pre-written prediction is a story, not an experiment — and recording failures is what keeps the record unfakeable | Adopted |
| 2026-09-16 | **Optimization session #1 — H1 (de-box `BookSide::best`) recorded as a NEGATIVE RESULT**: profiler attributed ~4.4% of samples to the boxed-iterator head, H1 predicted p50 250→~235–248 ns; after-run p50 250→250 ns with digest/counters/tests identical → the share was attribution blur (sampling + LTO + allocator reuse); the simplification ships with no performance claim | The one-hypothesis rule worked as designed: the written prediction failed, the failure is the record, and the next session's target is chosen from what survived (remove_order ~11.5% — a level-structure change, scoped honestly before any attempt) | Adopted |
| 2026-09-16 | **Optimization session #2 scoped (research + interrogation, zero code): Option A — slot arena + intrusive per-level chains (O(1) unlink/relink) behind the unchanged `OrderBook` API** — adopted as the only live design; **B (index-storing locators/swap-remove) measured dead** (attacks a ~4 ns scan), **C (flat price array) rejected** (needs a PriceOutsideBand rejection = core-semantics change, barred in Phase 2), **D (ghost levels) rejected** (≤ ~11 ns bound vs breaking the documented no-ghost-levels invariant). Revert bar (user-delegated): keep only on **≥5% p50** at 1M ops with digest/counters/tests identical; 2–4% or any wobble = revert-and-record | Evidence first: refined profile (remove_order ~14.8% self-time, memmove 1–3%) + a scratch micro-probe (descent 11–12 ns, full move pattern 81–97 ns, deque remove ~4 ns @ ~750 levels, ~1.3 orders/level) prove the cost is tree descents (double id→locator→level traversal + teardown), not the scan the obvious fix would attack; the pre-set revert bar exists precisely because H1 proved shares can be blur | Adopted |
| 2026-09-16 | **Optimization session #3 — Option A implemented, FAILED its prediction, REVERTED per the pre-set bar (second negative result)**: full arena + intrusive-chains + slot-locator rewrite landed with 100/100 unmodified tests, digest/counters byte-identical — and p50 250→250 ns (p90/p99 unmoved in the clean run). The Phase 0 `BTreeMap<Price, VecDeque>` book stands as final for this scale; the plateau rule (2 sessions, no p50 movement) has met its condition. Mistake-check on the fresh code caught two real chain-corruption bugs before any run (stale `next` links on moved orders; emptied-level tail reset) — proof the unmodified property suites are the right safety net | Three-experiment pattern: isolated micro-costs (4 ns scan, 12 ns descent) were real but do not compose additively end-to-end — at 250 ns/op the ensemble (timer ~27% by profile, risk/lock checks, hash, generator bookkeeping) dominates and out-of-order execution hides tree-op latency; honest scale-up: per-op micro-costs matter when ops are 10× larger, i.e. Phase 3 fees/journaling or Phase 4 — the implementation sketch is recorded for then, not kept as debt now | Adopted |
| 2026-09-16 | **v2 target CONFIRMED — met at baseline, before any optimization**: throughput 500k ops/sec target vs **~3.2 M ops/s measured** (criterion, mixed 9/3/6/82; 6.4× over target; the 1M+ stretch also exceeded at 3.2×); p99 < 100 µs target vs **667 ns measured** (~150× under target; the v1 <1 ms bar met 1,500×); replay determinism "identical state" — proven (two-run digest equality per run, cross-architecture digest match M1↔x86_64). The Phase 0-era targets were set before the engine existed and calibrated against the 2012-Xeon reference; the measured baseline outruns them all. The reference's 5M single-book figure stays the honest horizon (~1.6× ahead) **with the standing comparability disclosure**: our scope includes generator bookkeeping and risk handling theirs defers/excludes. No new stretch targets are invented now (target-shopping); Phase 3+ re-baselines under heavier per-op workloads where the profiled micro-costs become decisive | The TODO item existed precisely to prevent optimizing toward a target the baseline already beats — confirming it closes the optimization mandate honestly: the plateau rule met, targets met, the number published with methodology | Confirmed |
| 2026-09-16 | **Phase 2 whole-phase review passed (code-review-and-quality, five axes over the full Phase 2 diff `9a5571f..02c433f`) — approve, no Critical/Required findings.** Correctness clean (every run self-validates: mirror/engine agreement, conservation, digest equality *asserted* not printed; generator bugs panic loudly; p99.99 sample-gated; mirror swap-remove edge cases sound); readability clean (every non-obvious decision documented with its why in place); architecture clean (support/ pattern correct, benches touch public API only, engine.rs not grown — Phase 1's optional finding stays dormant, and the reverted arena left zero debt: book.rs carries only the `best()` simplification + provenance docs); security clean (CI least-privilege `contents: read`, dev-only deps one decision row each, `unsafe` forbid untouched); performance discipline held (methodology-before-numbers twice, two negative results recorded instead of kept, CI barred from publishing numbers — a bar proven necessary by the 2,845→5,010 ns runner swing). **Optional findings deferred:** (1) decompose `moves_rejected` into its three causes (clamped no-op / dead target / WouldCross); (2) lift `MIN_SAMPLES_P9999`/`DEFAULT_OPS` into support at a third harness consumer (third-use rule). **Carry-forward from Phase 1, still open:** the replay digest could fold book depth for marginally stronger evidence. Phase 2's Done-when triple met: harness runs reproducibly, methodology published, honest number recorded (v1.1) | The review's job is to catch what the phase's own discipline missed; here it confirmed the discipline held under adversarial reading — self-validating runs, disclosed scopes, pre-set revert bars — and the optional findings are recorded where the next harness revision will find them | Adopted |
| 2026-09-16 | **Phase 2 closed; Phase 3 (risk + event sourcing) opened.** Gates: whole-phase review (approve), v2 target confirmed met-at-baseline, fluency gate **passed** (2026-09-16, user-declared, self-administered — the user reports answering and checking all three items unscripted: the disclosed per-op scope + comparability disclosure; the two negative results and what the three experiments proved; digest-proven determinism and the CI-asserts-correctness-never-numbers rule; exact phrasing appendable on request). Carry-forwards into Phase 3: `reduceOrder` (Phase 3 by the adopt/skip list), the replay-digest book-depth question, and the bench two-run digest machinery as the replay-test precedent. The optimization sketch (slot arena + intrusive chains) is banked in the session record for Phase 3/4 scale | All three gates are green and the Done-when triple is met — closing now, with the honest note that the fluency pass is self-reported, is exactly what "record, don't polish" means; Phase 3's replay work inherits a proven determinism harness rather than building one | Adopted |
| 2026-09-16 | **Phase 3 ledger audit complete; self-match prevention ruled OUT of Phase 3 scope (→ Phase 4)**: the audit found the phases.md Phase 3 list ("balances, position limits, maker/taker fees; journal + snapshots + replay") already half-built — balances + the over-commitment risk gate are the Phase 0/1 ledger, more than the phase list admits — but book.rs's header claimed self-match prevention as "Phase 3 risk-control work" while phases.md's Phase 3 never mentions it. Ruling: the header yields; prevention lands in Phase 4 alongside uid gating (both become load-bearing when untrusted strangers share the engine). Fees, not prevention, define self-trade money treatment NOW | Phases.md is the scope of record; a code comment inventing scope is drift — the audit exists to catch exactly this. The money-relevant half (self-trades pay fees on both receipts) belongs to the fees slice regardless of phase | Adopted |
| 2026-09-16 | **Fee model ADOPTED: fees charged in the RECEIVED asset, integer basis points, per-currency fee sink in the ledger, conservation extended to Σ(free+locked)+Σ(fees)=deposits.** `FeeSchedule { maker_bps, taker_bps }` validated ≤ 10_000; per fill the resting order is maker / incoming is taker (roles the book already assigns); each side's fee = floor(received × role_bps / 10_000) — buyer's fee in base lots, seller's in quote ticks; self-trades pay fees on both receipts (no special case); rounding always floors (the exchange under-collects on dust, never over-charges); benches stay 0-fee so v1.1 numbers and digests stay comparable; the schedule is engine configuration that the journal will record for replay | Received-asset fees need no extra lock headroom: the lock covers the delivered asset (unchanged), the fee comes out of proceeds — the fee-on-top alternative would force role-dependent headroom into place-time locks, unresolvable for a GTC bid that can fill partly as taker (sweep) and partly as maker (rested). Floor rounding mirrors the ceil-lock/floor-settle asymmetry: never over-charge. The plan (test list included) is recorded in tasks/todo.md as the next session's contract | Adopted |
| 2026-09-16 | **Fees IMPLEMENTED per row 52** — `FeeSchedule` (integer bps, validated ≤ 10_000, floor rounding), per-currency fee sink (`collect_fee`/`fees_collected`), fee legs after both settle legs dispatched on `taker_side` (self-trades pay both receipts), fees never touch locks by construction, `Engine::new` stays zero-fee / `Engine::with_fees` opts in. 106 lib tests (6 new fee tests) + fee'd property twins green; 0-fee byte-identity **proven**: digest `0xc3bea4a3…` and all realized counters match v1.1 exactly. The post-edit gates caught **3 bugs before commit**: a botched edit had clobbered `settle`'s payee-credit branch (found via conservation failures in pre-existing tests — repaired byte-identical from HEAD), the overflow test's expected constant was 100× off (floor(2^64−1 × 10/10_000) = 18_446_744_073_709_551, not …955_161), and the first fee-leg draft computed both fees on base lots — the seller's fee must key on `quote_amount` ticks | The three catches are the test discipline working as designed: the settle clobber was invisible in the new fee tests but Conservation-after-every-op flagged it instantly; the wrong-asset fee base passed compile and would only show in cross-currency fee magnitudes — reading the failing test's arithmetic beat guessing. Bench digest proof is what licenses the fees slice as non-invasive | Adopted |
| 2026-09-16 | **Position limits ADOPTED: per-(user, currency) caps on LOCKED funds, checked in `Ledger::commit` before any mutation.** Default uncapped (zero cost for uncapped traffic; every pre-existing test/property unchanged); `set_position_limit` / `clear_position_limit` / `position_limit` accessor; `cap = 0` is a legal full freeze (a cap is a rule about future commitments, not an amount moved — the zero-amount rejection does not apply); a cap set below the current lock freezes growth while releases run off normally. The bid-move top-up path pre-checks the cap explicitly, because its release-then-commit sequence must never fail mid-way (a failed commit after release would strand the order lockless) — the pre-check replicates commit's rule for the post-release state and is documented at both sites. New `RiskError::PositionLimitExceeded { currency, cap, would_lock }`; every rejection path tested (at-cap pass, over-cap reject-untouched, pre-existing locks counted, freeze-below-lock, cap=0, per-account/currency independence, set/clear, book-never-sees-it, move-into-cap with nothing moved, move-under-cap fine) | The risk being capped is open-order exposure, not wealth — free funds are deposited money. The gate lives in `commit` because that is the only lock-growth choke point (place + the move top-up), so uncapped paths cannot bypass it. The move pre-check trades a little rule duplication for a structural guarantee: money-in-motion paths must never be able to fail half-way | Adopted |
| 2026-09-16 | **`reduceOrder` IMPLEMENTED (the Phase 2 adopt-deferred item): `Book::reduce` + `Engine::reduce_order`, clamp-to-remaining, removal when fully reduced, queue position preserved on partial reduces.** Money rule mirrors a fill of the same size exactly: a bid releases `floor(reduced × price)` quote ticks (the ceiling dust stays with the remainder and returns at order death — the settlement convention), an ask releases exactly `reduced` lots; `by ≥ remaining` IS a cancel (whole lock incl. dust, book + live map + index all cleaned). Floor subadditivity proves the remaining lock covers the remaining obligation for ANY split. No fills ⇒ no fee legs, no counterparty state touched. `ReduceOutcome { reduced (clamped), remaining, removed }` returned to callers; the clamp lives in `BookSide::reduce_order` so the invariant cannot be bypassed. Bench generator UNCHANGED — the canonical mix and digest stay frozen at v1.1; reduce enters workloads only with a future baseline v2 | Exchange-core's semantics adopted verbatim (decrease by N, clamped, remove at zero) plus the money half they don't model (reserve-based locks). Priority preservation is the honest reading: a reduce is not a reprice, and a re-queue would let a user jump their own queue by reducing+resting. The dust convention is deliberately the SAME rule settlement uses — one rounding story, not two | Adopted |
| 2026-09-16 | **Fee'd bench mode ADOPTED: `LAUNCHPAD_BENCH_FEES="maker,taker"` (bps) configures the bench engine's schedule; unset keeps the zero schedule so canonical runs stay byte-identical to v1.1 (re-proven: 1M digest `0xc3bea4a3b66dd253` + all counters identical).** `assert_conservation` (bench support) gains the fee-sink term — valid under both schedules. The bench `state_digest` is UNCHANGED and sufficient: conservation pins the per-currency sink given user balances, so two-run digest equality + conservation prove fee'd determinism completely. Same-session 1M comparison (0-fee vs 100% fees): p50 250 → 250 ns — the written prediction (p50 unchanged; fills are 4.9% of ops and p50 is set by the move path) CONFIRMED; p99 667/625 → 709/708 sits inside demonstrated noise bands and is claimed as nothing. Honest find: at 10/25 bps the fee'd digest EQUALS the 0-fee digest — bench fills (~25 lots @ ~1M ticks) floor realistic fees to exactly zero; the legs run, the amounts are legitimately 0. Fee-leg overhead is therefore exercised at 100% bps as an upper bound, disclosed as such; fee'd numbers are never compared to the canonical baseline (different configuration) | Fee'd measurement without config poisoning: the env var is read at engine construction, panics loudly on a malformed spec (proven live), and the default path is bit-for-bit today's engine. The zero-floor find is the floor rule doing its job at bench scale — tiny fills owe nothing, by design rather than by bug | Adopted |

| 2026-09-17 | **Phase 3 mid-phase review passed (code-review-and-quality, five axes over the Phase 3 diff `1e2bcb2..9514560` — fees, position limits, reduceOrder, fee'd bench mode + records).** Correctness clean: every money mutation pre-checked or expect-guarded with the invariant named at the scene (commit all-or-nothing; fee ≤ just-credited proceeds by the floor rule; reduce clamp at the book's invariant site; floor-subadditivity lock argument); fee legs dispatched on `taker_side` so self-trades are correct by construction; edge matrix covered (cap=0 freeze, cap-below-lock, at-cap pass, dusty-lock split releases, u64::MAX fee, 10_000 bps boundary, clamped reduce). Readability clean: every non-obvious rule carries its why at the site; no dead code from the mid-session bugs (the settle clobber repaired byte-identical, the first-draft dead branch removed). Architecture clean: risk rules in the ledger, money orchestration in the engine, the book stays money-blind; bench fee config reads env at engine construction in support (single wiring point, all three harnesses inherit). Security clean: no new deps, `unsafe` forbid untouched, `LAUNCHPAD_BENCH_FEES` parsed + validated + loud panic (fail-fast config, never a silent fallback). Performance clean: the limit gate + fee legs are inside the measured canonical run (1M digest `0xc3bea4a3b66dd253`, p50 250 ns unchanged; 100%-fee upper bound disclosed). **Optional findings deferred:** (1) engine.rs (2,216 lines) and risk.rs (1,255) are past the size-inspection signal — split test module / operation groups at the journal slice's natural seam, before piling on; (2) the move-path cap pre-check duplicates commit's rule in two places — justified by the never-fail-mid-way guarantee, but a third money-in-motion path must extract the shared would-lock rule. Carry-forward stays open: replay digest folding book depth — directly relevant now that the journal slice is next | The review confirmed the phase's own discipline held (pre-written decisions, digest-proven non-invasiveness, post-edit gates catching 3 bugs); the size finding is the honest one — the journal slice is the right moment to decompose, not after | Adopted |

| 2026-09-17 | **Test modules split out of the implementation files (review finding 1 executed): `engine/tests.rs` (1,497 lines) + `risk/tests.rs` (643), leaving engine.rs at 719 and risk.rs at 612 — all four under the ~1,000-line inspection signal.** Pure mechanical move behind the same `mod tests` path (`#[cfg(test)] mod tests;` + sibling file): the test source moved verbatim with one dedent, no `pub`/visibility changes, no content edits; fmt re-normalized the new files. Proof of behavior-invisibility: fmt/clippy `-D warnings` clean, **123 + 4 + 3 tests identical**, and the 2k-op latency digest **byte-identical pre- vs post-split** (`0xd736d89a4102f8b4` both sides, measured against HEAD at the same op count in the same session, not a stale recorded number). Review finding 2 consciously NOT executed: its trigger is a *third* money-in-motion path; only two sites exist, and extracting a shared rule now would violate the third-use rule the finding itself cited — recorded as the standing trigger | The review's standing instruction was to decompose at the journal slice's seam BEFORE piling on — done now, so the journal slice starts from files that fit; the digest proof is the same machinery that licensed the fees slice as non-invasive | Adopted |
| 2026-09-17 | **Command journal DESIGN ADOPTED (commands, not events; in-memory this slice, disk + snapshots next).** Journal = header (pair + fee schedule — engine config replay must restore) + command stream. `Command` mirrors the engine's public input surface 1:1 (Deposit/Withdraw/SetPositionLimit/ClearPositionLimit/Place-with-reserve/Cancel/Reduce/Move); `record` is **infallible by protocol** — every command's outcome is deterministic, so acceptance AND semantic rejection are both recorded entries (a fallible record would mean unrecorded state change replay could never reproduce). Replay runs commands through the ONE engine — no second, event-applying implementation that can drift (the exchange-core lesson applied to our stack). `JournalError::Divergence` fires when a recorded acceptance disagrees with replay: a bug or corruption, never papered over. The deep digest folds header config + all accounts + fee sink + position limits + live orders + **book depth both sides in match order with per-level queue order** — the Phase 1 digest-depth carry-forward, landed here, making price-time priority replay-observable. New observability: `OrderBook::depth` + the `Depth` snapshot type, `Ledger::accounts/fee_sink/position_limits`, `Engine::accounts/quote/base` | A second matching implementation is how event-sourced systems drift; command replay through the single engine makes identity provable instead of assumed. Depth in the digest closes the carry-forward honestly — an aggregate fold cannot see a queue-order violation | Adopted |
| 2026-09-17 | **Command journal IMPLEMENTED and the Done-when PROVEN: `core/src/journal.rs` + `journal/tests.rs` (8 tests, incl. the 120-command randomized replay-identity property).** Public surface: `Journal::for_engine/new`, `record` (infallible, returns acceptance), `apply`, `replay_into`, `replay`, `JournalHeader::of/to_engine/describe`, `deep_digest`. All engine behavior untouched — proof: canonical 2k-op digest **byte-identical** (`0xea9d71ec0f18e3a3`), 131 + 4 + 3 tests green, clippy `-D warnings` clean. Split convention honored: journal.rs 455 lines + tests.rs 392, both under the size signal. Honest bug story, caught by the gates before any commit: (1) the first draft's scenarios never funded BTC — asks lock base, so every ask died at risk and four tests failed (depth test, hand scenario's sanity, the proptest); (2) the scripted rejection test exposed an incoherent record contract (trading rejections recorded, money rejections escaping as `Err`) — fixed at the ROOT by making `record` infallible-by-protocol rather than special-casing the test; (3) two new-ledger-accessor pattern bugs and a `fee_sink`/`fees_collected` name confusion, all compile- or test-caught. Ledger `deposit/withdraw` now behave uniformly in the journal: outcome recorded, state never half-mutated | The Done-when is the phase's own words: replay produces identical state, proven by the test — deep digest equality on random streams, hand streams, and the empty journal | Adopted |

## Template row

| YYYY-MM-DD | [What was decided/done] | [Why — one line] | [Adopted / Confirm at / Rejected] |

## How to add an entry

- Date = today.
- Decision = the thing you chose or did (one line).
- Rationale = why (one line, honest).
- Status = Adopted / Confirm at [phase] / Rejected / Deferred.
- Chronological order, newest at the bottom — one table, no rows outside it.
